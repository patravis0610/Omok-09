using UnityEngine;

public class Constants : MonoBehaviour
{

    public enum GameType { SinglePlay, DualPlay, MultiPlay }

    public enum PlayerType { None, Player_Black, Player_White }

    public const int BlockColumnCount = 15;
}
